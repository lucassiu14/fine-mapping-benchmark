% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapper_funmap.R
\name{run_funmap_region}
\alias{run_funmap_region}
\title{Run Funmap on a single region from simulation data structures}
\usage{
run_funmap_region(region_geno, region_pheno, ...)
}
\arguments{
\item{region_geno}{List. One element of \code{simulation$genotypes},
containing \code{LD} and \code{n}.}

\item{region_pheno}{List. One element of a scenario's \code{regions},
containing \code{z} and \code{annotations_matrix}.}

\item{...}{Additional arguments passed to \code{\link{run_funmap}}
(e.g. \code{L}, \code{max_iter}, \code{tol}).}
}
\value{
The output of \code{\link{run_funmap}}, or an error result if no
annotations are available.
}
\description{
Extracts inputs from \code{region_geno} and \code{region_pheno} and calls
\code{\link{run_funmap}}. If no annotations matrix is present in
\code{region_pheno}, returns an informative error result without running
the method.
}
\details{
Annotations are produced by \code{run_simulation()} when
\code{annotations = "binary"} or \code{annotations = "continuous"} is
specified. If \code{annotations = "none"} was used (the default), Funmap
cannot be run.
}
