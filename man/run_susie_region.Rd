% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapper_susie.R
\name{run_susie_region}
\alias{run_susie_region}
\title{Run SuSiE on a single region from simulation data structures}
\usage{
run_susie_region(region_geno, region_pheno, use_individual = FALSE, ...)
}
\arguments{
\item{region_geno}{List. One element of \code{simulation$genotypes},
containing at minimum \code{LD}, \code{n}, and optionally \code{X}.}

\item{region_pheno}{List. One element of a scenario's \code{regions},
containing at minimum \code{z} and optionally \code{y}.}

\item{use_individual}{Logical. If TRUE and both \code{X} and \code{y} are
available, use individual-level data instead of summary statistics.
Default: FALSE.}

\item{...}{Additional arguments passed to \code{\link{run_susie}}
(e.g. \code{L}, \code{coverage}, \code{min_abs_corr}).}
}
\value{
The output of \code{\link{run_susie}}.
}
\description{
Thin adapter that extracts the appropriate inputs from the simulation's
\code{region_geno} and \code{region_pheno} objects and calls
\code{\link{run_susie}}. This is the function registered in the method
registry and called by \code{\link{run_methods}}.
}
